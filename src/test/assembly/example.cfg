MEMORY {
    ZP:          start = $0000, size = $0100, type = rw;
    DATA:        start = $0200, size = $0100, type = rw;
    RAM:         start = $0300, size=$7D00, type = rw;
    ROM:         start = $8000, size=$7FFA, type = ro, fill = yes;
    ROM_VECTORS: start = $FFFA, size=6, type = ro, fill = yes, fillval = $FF;
}

SEGMENTS {
    ZEROPAGE: load=ZP, type=zp;
    DATA:     load=DATA, type=rw;
    CODE:     load=ROM, type=ro;
    VECTORS:  load=ROM_VECTORS, type=ro;
}
